import Stripe from 'npm:stripe@17.5.0';
import {createClient} from 'npm:@supabase/supabase-js@2';
import {corsHeaders} from '../_shared/cors.ts';
type Item={ticketTypeId:string;quantity:number};
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});let reservations:{listingId:string;buyerId:string}[]=[],admin:ReturnType<typeof createClient>|undefined;try{const auth=req.headers.get('Authorization');if(!auth)throw Error('Non authentifié');const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:auth}}}),{data:{user}}=await db.auth.getUser();if(!user)throw Error('Non authentifié');const {data:profile}=await db.from('profiles').select('role').eq('id',user.id).single();if(profile?.role!=='user')throw Error('Ce compte ne peut pas acheter de billets');const {items}=await req.json() as {items:Item[]};if(!items?.length||items.some(item=>!item.ticketTypeId||!Number.isInteger(item.quantity)||item.quantity<1)||items.reduce((sum,item)=>sum+item.quantity,0)>10)throw Error('Sélection invalide');const ids=items.map(item=>item.ticketTypeId);let {data:types}=await db.from('ticket_types').select('id,name,price_cents,quantity,sold_quantity,sales_cutoff_at,event_id,events!inner(id,title,organizer_id,status)').in('id',ids);if(!types||types.length!==items.length)throw Error('Billet indisponible');const firstEvent=(types[0] as any).events;if(types.some((type:any)=>type.event_id!==firstEvent.id||type.events.status!=='published'))throw Error('Sélection invalide');admin=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const rollover=await admin.rpc('rollover_expired_ticket_phase_capacity',{p_event:firstEvent.id});if(rollover.error)throw rollover.error;const refreshed=await db.from('ticket_types').select('id,name,price_cents,quantity,sold_quantity,sales_cutoff_at,event_id,events!inner(id,title,organizer_id,status)').in('id',ids);if(refreshed.error||!refreshed.data)throw Error('Billet indisponible');types=refreshed.data;if(types.some((type:any)=>new Date(type.sales_cutoff_at)<=new Date()))throw Error('La vente de cette phase est terminée');const primaryItems:Item[]=[],resaleListings:{listingId:string;feeCents:number}[]=[];for(const item of items){const type:any=types.find(row=>row.id===item.ticketTypeId),reserved=await admin.rpc('reserve_public_ticket_resales',{p_ticket_type:item.ticketTypeId,p_buyer:user.id,p_quantity:item.quantity});if(reserved.error)throw reserved.error;for(const listing of reserved.data??[]){resaleListings.push({listingId:listing.listing_id,feeCents:listing.fee_cents});reservations.push({listingId:listing.listing_id,buyerId:user.id})}const remaining=item.quantity-(reserved.data?.length??0);if(type.sold_quantity+remaining>type.quantity)throw Error('Une phase n’est plus disponible');if(remaining)primaryItems.push({ticketTypeId:item.ticketTypeId,quantity:remaining})}const stripeKey=Deno.env.get('STRIPE_SECRET_KEY'),publishableKey=Deno.env.get('STRIPE_PUBLISHABLE_KEY');if(!stripeKey||!publishableKey)throw Error('Configuration Stripe manquante');const stripe=new Stripe(stripeKey),{data:org}=await admin.from('organizer_profiles').select('stripe_account_id,onboarding_complete,primary_fee_bps,primary_fee_min_cents').eq('profile_id',firstEvent.organizer_id).single();if(!org?.stripe_account_id||!org.onboarding_complete)throw Error('Le compte Stripe de l’organisation n’est pas opérationnel');const amount=items.reduce((sum,item)=>sum+Number((types as any[]).find(type=>type.id===item.ticketTypeId).price_cents)*item.quantity,0),primaryFee=primaryItems.reduce((sum,item)=>{const type:any=(types as any[]).find(row=>row.id===item.ticketTypeId),fee=Math.min(type.price_cents,Math.max(Math.round(type.price_cents*(org.primary_fee_bps??350)/10000),org.primary_fee_min_cents??49));return sum+fee*item.quantity},0),applicationFee=primaryFee+resaleListings.reduce((sum,item)=>sum+item.feeCents,0);const intent=await stripe.paymentIntents.create({amount,currency:'eur',automatic_payment_methods:{enabled:true},receipt_email:user.email,application_fee_amount:applicationFee,transfer_data:{destination:org.stripe_account_id},metadata:{kind:'mobile_checkout',items:JSON.stringify(primaryItems),resaleListings:JSON.stringify(resaleListings.map(item=>item.listingId)),buyerId:user.id,eventId:firstEvent.id}});for(const reservation of reservations){const attached=await admin.rpc('attach_resale_checkout',{p_listing:reservation.listingId,p_buyer:user.id,p_session:intent.id});if(attached.error){await stripe.paymentIntents.cancel(intent.id);throw attached.error}}return Response.json({paymentIntentClientSecret:intent.client_secret,publishableKey,merchantDisplayName:'NOCTURNE'},{headers:corsHeaders})}catch(error){if(admin)for(const reservation of reservations)await admin.rpc('release_resale_reservation',{p_listing:reservation.listingId,p_buyer:reservation.buyerId});return Response.json({error:error instanceof Error?error.message:'Erreur de paiement'},{status:400,headers:corsHeaders})}});
