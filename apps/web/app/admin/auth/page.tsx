'use client';
import { FormEvent, useEffect, useState } from 'react';
import { LoaderCircle, ShieldCheck } from 'lucide-react';
import { getSupabase } from '../../../lib/supabase-browser';

export default function AdminAuthPage(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{void (async()=>{const supabase=getSupabase(),{data}=await supabase.auth.getUser();if(!data.user)return;const {data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).single();if(profile?.role==='admin')location.assign('/admin/dashboard')})()},[]);async function submit(event:FormEvent){event.preventDefault();setLoading(true);setError('');const supabase=getSupabase(),{data,error:authError}=await supabase.auth.signInWithPassword({email,password});if(authError){setError('Identifiants incorrects.');setLoading(false);return}const {data:profile}=await supabase.from('profiles').select('role').eq('id',data.user.id).single();if(profile?.role!=='admin'){await supabase.auth.signOut();setError('Ce compte ne fait pas partie de l’administration NOCTURNE.');setLoading(false);return}location.assign('/admin/dashboard')}return <main className="admin-auth"><form onSubmit={submit}><ShieldCheck/><p className="eyebrow">ACCÈS INTERNE</p><h1>Administration NOCTURNE</h1><label>Adresse e-mail<input type="email" required value={email} onChange={event=>setEmail(event.target.value)}/></label><label>Mot de passe<input type="password" required value={password} onChange={event=>setPassword(event.target.value)}/></label>{error&&<p className="profile-error">{error}</p>}<button className="cta" disabled={loading}>{loading?<LoaderCircle className="spin"/>:<ShieldCheck/>}Se connecter</button></form></main>}
