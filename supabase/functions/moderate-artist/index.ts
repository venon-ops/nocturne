import { createClient } from 'npm:@supabase/supabase-js@2'; import { corsHeaders } from '../_shared/cors.ts';
Deno.serve(async req=>{try{const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:req.headers.get('Authorization')!}}});const {data:{user}}=await db.auth.getUser();const {data:me}=await db.from('profiles').select('role').eq('id',user?.id).single();if(me?.role!=='admin')throw Error('Forbidden');const {artistId,approved}=await req.json();const admin=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);await admin.from('profiles').update({role:approved?'artist_verified':'user'}).eq('id',artistId);return Response.json({ok:true},{headers:corsHeaders})}catch(e){return Response.json({error:e.message},{status:403,headers:corsHeaders})}});
