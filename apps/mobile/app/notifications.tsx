import {router} from 'expo-router';import {useEffect,useState} from 'react';import {ActivityIndicator,Pressable,ScrollView,StyleSheet,Text,View} from 'react-native';import {supabase} from '../lib/supabase';
import RoundBackButton from '../components/RoundBackButton';
type Item={id:string;type:string;payload:Record<string,unknown>;read_at:string|null;created_at:string};
const label=(item:Item)=>item.type==='ticket_resold'?['Billet revendu',`Ton remboursement de ${((Number(item.payload.refund_cents)||0)/100).toLocaleString('fr-FR',{style:'currency',currency:'EUR'})} est en cours.`]:item.type==='waitlist_batch_available'?['Billets disponibles','Ton lot est réservé pendant 30 minutes.']:item.type==='waitlist_payment_action_required'?['Paiement à confirmer','Ta banque demande une confirmation supplémentaire.']:['Notification NOCTURNE','Une nouvelle information est disponible.'];
export default function Notifications(){const [items,setItems]=useState<Item[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/auth');return}const {data}=await supabase.from('notifications').select('id,type,payload,read_at,created_at').order('created_at',{ascending:false}).limit(100);setItems((data??[]) as Item[]);setLoading(false)})()},[]);async function read(id:string){const now=new Date().toISOString();await supabase.from('notifications').update({read_at:now}).eq('id',id);setItems(current=>current.map(item=>item.id===id?{...item,read_at:now}:item))}if(loading)return <View style={s.center}><ActivityIndicator color="#53F6D4"/></View>;return <ScrollView style={s.page} contentContainerStyle={s.content}><RoundBackButton/><Text style={s.eyebrow}>MON ESPACE</Text><Text style={s.title}>Notifications.</Text>{!items.length?<Text style={s.empty}>Aucune notification pour le moment.</Text>:items.map(item=>{const [title,body]=label(item);return <Pressable key={item.id} style={[s.card,item.read_at&&s.read]} onPress={()=>void read(item.id)}><View style={s.cardHead}><Text style={s.cardTitle}>{title}</Text>{!item.read_at?<Text style={s.new}>NOUVELLE</Text>:null}</View><Text style={s.body}>{body}</Text><Text style={s.date}>{new Date(item.created_at).toLocaleString('fr-FR')}</Text></Pressable>})}</ScrollView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:'#080A14'},content:{padding:22,paddingTop:64,paddingBottom:50},center:{flex:1,alignItems:'center',justifyContent:'center',backgroundColor:'#080A14'},back:{color:'#9FA3B8'},eyebrow:{color:'#53F6D4',fontSize:10,fontWeight:'900',letterSpacing:1.4,marginTop:40},title:{color:'#F8F7FF',fontSize:38,fontWeight:'900',letterSpacing:-2,marginTop:10,marginBottom:20},empty:{color:'#9FA3B8',padding:35,textAlign:'center'},card:{padding:17,borderWidth:1,borderColor:'#31584F',borderRadius:16,backgroundColor:'#101B1D',marginBottom:10},read:{borderColor:'#292D42',backgroundColor:'#101321',opacity:.7},cardHead:{flexDirection:'row',justifyContent:'space-between',gap:10},cardTitle:{color:'#F8F7FF',fontWeight:'900'},new:{color:'#FF4D99',fontSize:8,fontWeight:'900'},body:{color:'#9FA3B8',fontSize:12,lineHeight:18,marginTop:7},date:{color:'#676C80',fontSize:9,marginTop:9}});
