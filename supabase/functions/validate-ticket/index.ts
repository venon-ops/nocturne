import { createClient } from 'npm:@supabase/supabase-js@2'; import { corsHeaders } from '../_shared/cors.ts';
Deno.serve(async req=>{try{const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{global:{headers:{Authorization:req.headers.get('Authorization')!}}});const {data:{user}}=await db.auth.getUser();if(!user)throw Error('Unauthorized');const {qrToken}=await req.json();const {data,error}=await db.rpc('validate_ticket_token',{p_token:qrToken,p_scanner:user.id});if(error)throw error;return Response.json({data:data?.[0]},{headers:corsHeaders})}catch(e){return Response.json({error:e.message},{status:400,headers:corsHeaders})}});
